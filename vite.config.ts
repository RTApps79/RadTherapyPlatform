import { defineConfig } from 'vite';

export default defineConfig({
  // This tells Vite: "When I deploy, my code is sitting in a subfolder called /RadTherapyPlatform/"
  base: '/RadTherapyPlatform/',
});
